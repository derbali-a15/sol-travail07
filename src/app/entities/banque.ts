export class Banque {
    id: number
    nom: string
    ville: string
    constructor(id: number, nom: string, ville: string) {
        this.id = id
        this.nom = nom
        this.ville = ville
    }
}
